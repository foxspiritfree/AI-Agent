# Imported Reference Packages

這裡放外部導入素材與參考包。

這些資料不是目前正式啟用的 `skills/` 或 `workflows/`，除非使用者明確要求，AI 不應把這裡的內容當成當前執行規則。

## Current Packages

- `pro_kit_imported/`：AI 分身起始助手與 pro-kit 參考素材，已從 `000_Agent/` 移出，保留作來源參考。
- `skills_imported/`：外部導入的 landing-page、learning-journal、social-cards skill 素材，已從 `000_Agent/` 移出；需要正式啟用時，再挑選後搬入 `000_Agent/skills/`。
