# system-build — 新系統開發完整工作流

這個快捷入口專為「從零到一」或「開發大型新功能」設計。
它將端到端（End-to-End）涵蓋問題釐清、PRD、UI/UX 設計、開發實作與交付。

**請直接呼叫並載入 skill**：`fullstack` 

當你載入 `fullstack` skill 後，請嚴格按照該 skill 中定義的 Phase 1 到 Phase 6 逐步執行。你不需要再另外手動呼叫 `pm` 或 `uiux`，所有的開發流程都已經收斂在 `fullstack` 這個巨型 skill 之中。
